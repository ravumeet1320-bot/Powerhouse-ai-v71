Replace the existing repository-root v75_app.py with this file.
Do not delete legacy V74.x files or static/v75.html.
Commit to main, then Render: Manual Deploy -> Clear build cache & deploy.
Start command must remain: uvicorn v75_app:app --host 0.0.0.0 --port $PORT
